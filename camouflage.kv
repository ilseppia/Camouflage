#:kivy 1.9.1
#:import os os

    
<Camouf>:
    canvas:
        Color:
            rgba: 0, 1, 1, 0.1
        Rectangle:
            size: self.size
            pos: 0,0

          

            
<GameLayout>:
    canvas:
        Color:
            rgba: 1, 1, 1, 1
        Rectangle:
            source: os.path.join('data','screen_border_back.png')
            size: self.size

            

            
            
<Test>:      

    Image:
        source: 'screen_border_front.png'
        
    BoxLayout:
        orientation: 'horizontal'
                
        Button_Camouf:
            size_hint: 0.1, 0.1
            text: 'body'
            #on_touch_down: root.change_camouf(self.text)
        Button_Camouf:
            size_hint: 0.1, 0.1
            text: 'hair'
            #on_touch_down: root.change_camouf(self.text)
        Button_Camouf:
            size_hint: 0.1, 0.1
            text: 'eyes'
            #on_touch_down: root.change_camouf(self.text)     
        Button_Camouf:
            size_hint: 0.1, 0.1
            text: 'nose'
            #on_touch_down: root.change_camouf(self.text)   
        Button_Camouf:
            size_hint: 0.1, 0.1
            text: 'mouth'
            #on_touch_down: root.change_camouf(self.text)               
    
    
        

